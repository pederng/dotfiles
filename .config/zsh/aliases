# vi:syntax=zsh
#Utilites
alias grep='grep --color=auto'
alias dmesg='dmesg -HL'
alias x='exit'
alias cp='cp -iv'
alias rm='rm -iv --one-file-system'
alias mv='mv -iv'
alias mkdir='mkdir -p -v'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'
alias tnew='new-tmux-from-dir-name'
alias tree='tree -C'

#ls
alias ls='ls -hF --color=auto'
alias lr='ls -R'                    # recursive ls
alias ll='ls -l'
alias la='ll -A'
alias lx='ll -BX'                   # sort by extension
alias lz='ll -rS'                   # sort by size
alias lt='ll -rt'                   # sort by date
alias lm='la | more'

alias gpgreset="gpg-connect-agent updatestartuptty /bye"

alias dc="docker-compose"
alias dcdev="dc -f docker-compose.yml -f docker-compose.dev.yml"
alias dcdeploy="dc -f docker-compose.yml -f docker-compose.deploy.yml"

alias https='http --default-scheme=https'

alias emacs="emacs -nw"
alias mutt='neomutt'

alias startx="startx $XINITRC"
alias tmux="tmux -f $XDG_CONFIG_HOME/tmux/tmux.conf"
alias gcalcli="gcalcli --config-folder=$XDG_CONFIG_HOME/gcalcli"

# dotfile storage config
alias dotfiles='git --git-dir=$XDG_DATA_HOME/dotfiles/ --work-tree=$HOME'
